apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-backend-chart.fullname" . }}
  labels:
    {{- include "todo-backend-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todo-backend-chart.selectorLabels" . | nindent 4 }}
